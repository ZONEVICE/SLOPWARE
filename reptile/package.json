{
  "name": "reptile",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time two-way directory synchronisation between two computers on a LAN, over HTTP or HTTPS. chokidar watches the disk; everything else is Node's standard library and a plain HTML/CSS/JS client.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  }
}
